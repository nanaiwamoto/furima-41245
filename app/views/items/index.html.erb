<div class="contents row">
  <% @items.each do |items| %>
    <div class="content_post" style="background-image: url(<%= items.image %>);">
      <p><%= tweet.text %></p>
      <span class="name">
        <%= items.name %>
      </span>
    </div>
  <% end %>
</div>
